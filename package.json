{
  "name": "schedule-board",
  "private": true,
  "scripts": {
    "test:rules": "node --test tests/firestore-rules.test.js",
    "verify": "node scripts/verify_release.js && node scripts/verify_rules_contract.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
